# Netlify Deployment Configuration for Quarto
# Site is pre-rendered locally and _site folder is deployed

[build]
  # No build command needed - site is pre-rendered locally
  command = ""
  publish = "_site"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
